export const base_url = 'https://ign-apis.herokuapp.com/';

export const articles_feed = `${base_url}articles?count=8`;
export const videos_feed = `${base_url}videos`;

///comments?ids={comma seperated contentIds}
export const comments_feed = `${base_url}comments?ids=`;
